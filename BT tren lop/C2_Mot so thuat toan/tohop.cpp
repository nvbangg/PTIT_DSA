#include <bits/stdc++.h>
using namespace std;
vector<int> a(100);
int n, k;
bool ok = 1;
void init()
{
    cin >> n >> k;
    for (int i = 0; i < k; ++i)
        a[i] = i + 1;
}
void out()
{
    for (int i = 0; i < k; ++i)
        cout << a[i] << " ";
    cout << endl;
}
void sinh()
{
    for (int i = a[i]; i +)
}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    init();
    while (ok)
    {
        out();
        sinh();
    }
}